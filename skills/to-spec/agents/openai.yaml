interface:
  display_name: "To Spec"
  short_description: "Summarize the session and draft an issue plan"
  default_prompt: "Use $to-spec to summarize our session and produce a readable spec with an issue map and full draft issue bodies in this chat."
policy:
  allow_implicit_invocation: false

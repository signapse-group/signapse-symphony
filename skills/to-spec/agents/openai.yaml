interface:
  display_name: "To Spec"
  short_description: "Synthesize a conversation into a structured spec"
  default_prompt: "Use $to-spec to synthesize our current conversation into a structured spec in this chat."
policy:
  allow_implicit_invocation: false
